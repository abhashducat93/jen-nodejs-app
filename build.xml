<target name="package" depends="build">
  <zip destfile="${dist.dir}/app-${DSTAMP}.zip" 
       basedir="." 
       includes="**/*" 
       excludes="node_modules/**, .git/**, build/**, dist/**"/>
</target>